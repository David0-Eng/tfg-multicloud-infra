name: Terraform CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [aws-dev, aws-ha, azure-dev]
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.14.9"

      - name: Format check
        run: terraform fmt -check -recursive

      - name: Init (no backend, no credentials needed)
        run: terraform -chdir=envs/${{ matrix.env }} init -backend=false

      - name: Validate
        run: terraform -chdir=envs/${{ matrix.env }} validate

  secret-scan:
    runs-on: ubuntu-latest
    steps:
      # Full history: a secret that was committed and later removed is still
      # in the repository, which is exactly how the webhook leaked.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Static security analysis of the IaC. soft_fail: findings are
      # reported without blocking the push; accepted ones are justified
      # in the thesis.
      - uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform
          soft_fail: true
